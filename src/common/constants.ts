export const VERIFICATION_COST = 50;
export const REWARD_AMOUNT = 20;
export const LOW_BALANCE_THRESHOLD = 200;
export const DEFAULT_VERIFICATION_COST = 50;
export const DEFAULT_REWARD_AMOUNT = 20;
export const DEFAULT_LOW_BALANCE_THRESHOLD = 200;
export const DEFAULT_WALLET_FUNDING_FEE = 100;
export const API_KEY_PREFIX = 'rain_live_';
/** Internal tenant for Rain ops / platform admins — not a customer institution. */
export const PLATFORM_INTERNAL_INSTITUTION_ID = 'inst_rain_platform';
export const PLATFORM_INTERNAL_INSTITUTION_EMAIL = 'platform@internal.rain';
export const IDEMPOTENCY_TTL_MS = 24 * 60 * 60 * 1000;
export const MONNIFY_CHECKOUT_TTL_SECONDS = 40 * 60;
